
`back`: Убрать всю валидацию с фронта, перенести её чисто на бек. (и не писать валидацию во фронте)

`front`: Сделать дизайн логина и регистрации по фигме.

`back`: Сделать models на беке для всех новых таблиц БД.

`front` (Взял Ваня): Сделать компонент для вывода уведомлений и осуществить возможность вызова уведомления из любой страницы (.tsx).

`back`: сделать в передаче вместе с токеном username

`front`: сделать получение username и сохранять его в куки (localstorage)

`front`: сделать справа сверху возможность войти в профиль, выйти, зарегаться, обязательно из куки (localstorage) получаем username и отображаем.

`figma`: (/) Сделать набросок главной страницы. Ещё лучше, если саму главную страницу. Наши плюсы, добавить кнопку "перейти к проектам" (/projects) и "создать проект"

`front`: разработать главную страницу в соответствии с дизайном.

`front`: настройки сайта сделать, входить справа сверху, возле значка пользователя, хранить всё в куки (localstorage).

`back`: разработать апи получения списка проектов, их сортировку в зависимости от query-параметров в get-запросе. Сортировку по дате открытия и создания проекта.

`figma`: (/projects) нарисовать страницу с проектами (с заголовками, датами и сортировкой), кнопкой создания проекта.

`back`: разработать апи создания проекта и сохранения его в БД, должен возвращаться класс проекта (как минимум его id). 

`figma`: (/projects/{project_id}/files) нарисовать страницу файлов проекта.

`back`: переделать апи загрузки файлов и разработать сохранение файлов в БД.

`back`: написать апи выгрузки файлов из БД и отправки на фронт.

`figma`: нарисовать овно для загрузки файлов на сервер. 

`front`: переписать апи-запрос для отправки файлов на сервер (сделать возможность передачи name-файла и project_id).

`back`: написать api для получения списка всех файлов проекта (только список!, без данных внутри), id проекта берём из get-запроса такого формата (/projects/{project_id}/files) 

`front`: написать апи-запрос для получения файлов с сервера и сделать их отображение (хотя бы названий) на странице файлов проекта.